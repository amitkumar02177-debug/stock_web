name: Keep Render Awake

on:
  schedule:
    - cron: '*/10 * * * *'  # every 10 minutes
  workflow_dispatch:  # allows manual run

jobs:
  ping_render:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render app
        run: |
          curl -s https://stock-web-52l4.onrender.com/ > /dev/null
          echo "Pinged Render app"
